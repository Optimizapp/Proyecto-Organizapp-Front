Módulo de procesos - Semana 1

Base creada:
- Ruta /processes
- Ruta /processes/:id
- Vista de lista de procesos
- Vista de detalle de proceso
- Servicio base con datos mock

Pendiente de integración backend:
- GET /api/processes
- GET /api/processes/{id}
- POST /api/processes
- PUT /api/processes/{id}
- DELETE /api/processes/{id}

Observación:
El servicio actual usa datos mock mientras se alinea la estructura final del backend.